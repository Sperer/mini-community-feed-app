import 'package:flutter/material.dart';

void main() {
  return runApp(MiniCommunityApp());
}

class MiniCommunityApp extends StatelessWidget {
  const MiniCommunityApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Container(),
    );
  }
}
